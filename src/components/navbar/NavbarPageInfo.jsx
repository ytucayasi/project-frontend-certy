import { useState } from "react";

const NavbarPageInfo = ({pageName}) => {
    return (
        <h1 className="font-raleway-bold text-2xl">
            {pageName}
        </h1>
    );
}

export default NavbarPageInfo;